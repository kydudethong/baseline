.rally_cache/
runs/
models/*.pt
models/*.onnx
assets/*.mp4
assets/*.npz
# Regenerated by tests/make_synthetic.py alongside the clip above, which is
# already ignored -- tracking half of one generated fixture set is worse than
# tracking none of it, because the tracked half silently goes stale.
assets/synthetic.detections.json
assets/synthetic.labels.json
assets/_debug/
*.debug.mp4
*.features.npz
__pycache__/
*.pyc
.rsvenv/
