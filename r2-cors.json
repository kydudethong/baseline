{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "https://baseline-court.fly.dev"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
